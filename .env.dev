# Local development. Pass it explicitly: docker compose --env-file .env.dev up -d
#
# --env-file replaces .env rather than layering on top of it, so every variable
# the stack needs has to be repeated here.

DATA_DIR=./data

SITE_DOMAIN=localhost
SITE_ALT_NAMES=""
SITE_CERT_NAME=localhost
