# Copy to .env.secrets (gitignored) before the first `docker compose up`.
# ~/setup.sh on the thevenin host generates this automatically.
#
# Only applied when the mysql data directory is empty. Rotating the password on
# a live database is a phpmyadmin / ALTER USER operation, not an edit here.
MYSQL_ROOT_PASSWORD=change-me
